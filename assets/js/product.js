const id=new URL(location).searchParams.get('id');async function load(){const products=await fetch('/api/products').then(r=>r.json()),p=products.find(x=>x.id===id);if(!p){productDetail.innerHTML='<p>Product not found.</p>';return}const image=p.image_urls?.[0]||'';productDetail.innerHTML=`<div class="form-grid"><div>${image?`<img src="${encodeURI(image)}" style="width:100%;max-height:420px;object-fit:contain" alt="">`:'<div class="product-placeholder" style="height:320px"></div>'}</div><div><p class="muted">${p.category}</p><h1>${p.title}</h1><div class="price">₦${Number(p.price).toLocaleString()}</div><p>${p.description}</p><p class="muted">Sold by ${p.seller_profiles?.business_name||'Trade Best seller'} · ${p.seller_profiles?.city||'Nigeria'}</p><a class="primary-btn" href="catalog.html">Add to cart from catalogue</a></div></div>`}load();
