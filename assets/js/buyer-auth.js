const message=document.getElementById('message');async function send(form,url){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),d=await r.json();message.textContent=d.message||'';if(r.ok&&d.ok)location='buyer-dashboard.html'}buyerLogin.addEventListener('submit',e=>{e.preventDefault();send(buyerLogin,'/api/buyer/login')});buyerSignup.addEventListener('submit',e=>{e.preventDefault();send(buyerSignup,'/api/buyer/signup')});
